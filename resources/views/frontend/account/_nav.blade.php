<div class="mini-panel p-3 mb-4">
    <div class="d-flex flex-wrap gap-2">
        <a href="{{ route('account.dashboard') }}" class="btn btn-sm btn-outline-secondary">Dashboard</a>
        <a href="{{ route('account.profile') }}" class="btn btn-sm btn-outline-secondary">Profile</a>
        <a href="{{ route('account.orders') }}" class="btn btn-sm btn-outline-secondary">Orders</a>
        <a href="{{ route('account.wishlist') }}" class="btn btn-sm btn-outline-secondary">Wishlist</a>
        <a href="{{ route('account.addresses') }}" class="btn btn-sm btn-outline-secondary">Addresses</a>
    </div>
</div>
